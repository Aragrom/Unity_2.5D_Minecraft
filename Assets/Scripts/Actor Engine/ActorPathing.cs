using System.Collections;
using System.Collections.Generic;
using Unity.Burst;
using UnityEngine;

[BurstCompile]
public struct Dijkstra
{ 

}

[BurstCompile]
public struct Astar
{ 

}

[BurstCompile]
public struct Dstar
{ 

}

[BurstCompile]
public class ActorPathing : MonoBehaviour
{
    // boolean map of terrain for fast ai pathing.
    public BlockEngine blockEngine = null;

    // Start is called before the first frame update
    [BurstCompile]
    void Start()
    {
        
    }

    // Update is called once per frame
    [BurstCompile]
    void Update()
    {
        
    }

    [BurstCompile]
    void SolveSubChunkPathing(int[] chunkNodeValues)
    { 
    
    }
}
