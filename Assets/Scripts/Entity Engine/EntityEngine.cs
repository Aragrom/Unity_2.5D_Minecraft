using System.Collections;
using System.Collections.Generic;
using Unity.Burst;
using UnityEngine;

// YOU HAVE NOT YET TRIED MAKE TERRAIN WERE THE SIZE OF ALL DATA NEVER CHANGES. ENTER EXTRA DATA IF NEEDED TO MAKE THE SIZE THE SAME. SO EMPTY TRIANGLE SPACE BUT SOME WIERD DATA IN OR SOMETHING!!!

[BurstCompile]
public class EntityEngine : MonoBehaviour
{
    /*public Main main = null;

    public CreateEntity createEntity = null;

    [BurstCompile]
    public void Awake()
    {
        main = GameObject.Find("Main").GetComponent<Main>();

        createEntity = GetComponent<CreateEntity>();
    }*/
}
